server:
  port: 8080

spring:
  application:
    name: gateway

  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: zapatillas-route
              uri: lb://ZAPATILLAS
              predicates:
                - Path=/api/v1/zapatillas/**

            - id: zapatillas-v2-route
              uri: lb://ZAPATILLAS
              predicates:
                - Path=/api/v2/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/